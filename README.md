# Generates /sitemap.xml for eZPublish 4.x

## Installation

* Clone extension into `extensions/sitemap`
* Map `/sitemap.xml` to `/sitemap/build` using mod rewrite or similar

## Dependencies

* [ezote](https://github.com/keyteqlabs/ezote)
